package impl;

public interface DuplicateRemoval {
    /**
     * Interface that maps the behavior for implementations that removes duplicated characters from the input string.
     * @param input to be checked
     * @return string without duplicate characters
     */
    String removeDuplicates(String input);
}
